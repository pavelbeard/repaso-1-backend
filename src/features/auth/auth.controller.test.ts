import { describe, it } from 'vitest'

describe('Auth Controller', () => {
  it('should login a user', () => {
    // Aquí iría la lógica para probar el login
  })

  it('should register a user', () => {
    // Aquí iría la lógica para probar el registro
  })
})
